using NUnit.Framework;
using UnityEngine;
//using TMPro;

public class FileExplorerTests
{
    [Test]
    public void OpenFile_ShouldSetLoadingPressCanvasText()
    {
        /*// Arrange
        var fileExplorer = new GameObject().AddComponent<FileExplorer>();
        var loadingPressCanvas = new GameObject().AddComponent<TMP_Text>();
        fileExplorer.loadingPressCanvas = loadingPressCanvas;

        // Act
        fileExplorer.OpenFile();

        // Assert
        Assert.AreEqual("Loading", loadingPressCanvas.text);*/
    }
}
